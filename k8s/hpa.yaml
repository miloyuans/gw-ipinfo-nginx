apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gw-ipinfo-nginx
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gw-ipinfo-nginx
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
