apiVersion: v1
kind: Service
metadata:
  name: global-googleplay-luodiye
  namespace: area
spec:
  selector:
    app: global-googleplay-luodiye
  ports:
    - name: http
      port: 80
      targetPort: 8080
